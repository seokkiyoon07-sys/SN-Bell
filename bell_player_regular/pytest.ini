# pytest 설정 파일
[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    slow: 느린 테스트를 표시
    integration: 통합 테스트를 표시
    unit: 단위 테스트를 표시 (기본값)